name: purple_portfolio
description: "퍼플포트: 주식 알림 - Flutter app (local SQLite + background alerts)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path_provider: ^2.0.14
  intl: ^0.18.1
  http: ^1.2.0
  flutter_local_notifications: ^17.1.2
  android_alarm_manager_plus: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
